# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Import Secure routes
*       /                                       module:secure

*		/signup                                 Signup.index


*		/vehicules/								Vehicles.myVehicles
*		/vehicules/ajouter						Vehicles.add
*		/vehicules/{id}							Vehicles.seeVehicle

*		/compte/								Members.seeProfile
*		/compte/editer							Members.profile

GET		/membres/								Members.findFriends
GET		/membres/{id}							Members.seeProfile

GET		/amis/									Members.friends

# Catch all
*		/{controller}/{action}                  {controller}.{action}
